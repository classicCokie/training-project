{
  "name": "training-project-connector",
  "version": "0.0.1",
  "description": "",
  "dependencies": {
    "@mobify/commerce-integrations": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "eslint": "4.19.1",
    "jest": "22.4.3",
    "jsdoc": "3.5.5"
  },
  "main": "index.js",
  "scripts": {
    "build": "babel src --bail --ignore '*.test.js','test.js' --out-dir dist --copy-files",
    "start": "babel src --bail --ignore '*.test.js','test.js' --out-dir dist --copy-files --watch",
    "prepare": "npm run build",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true
}
